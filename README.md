Template GD32E230 FreeRTOS / SPL project

Simply blink an LED

Example usage:

i.  Create build system: ```cmake -DCMAKE_BUILD_TYPE=Debug -B build```

ii. Build firmware: ```cmake --build build --target all```
